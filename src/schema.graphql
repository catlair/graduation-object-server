# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Auth {
  """JWT access token"""
  accessToken: String!

  """JWT refresh token"""
  refreshToken: String!
  user: User!
}

type Captcha {
  """captcha key"""
  key: String!

  """svg captcha"""
  svg: String!
}

type ChangeEmail {
  email: String!
  userId: String!
}

input ChangeEmailInput {
  code: String!
  email: String!
}

input ChangePasswordInput {
  code: String!
  email: String!
  newPassword: String!
  oldPassword: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Email {
  createdAt: DateTime!
  email: String!
  name: String!
}

input LoginInput {
  email: String
  id: Int
  password: String!
}

type Logout {
  message: String
}

type Mutation {
  changeEmail(data: ChangeEmailInput!): ChangeEmail!
  changePassword(data: ChangePasswordInput!): User!
  login(data: LoginInput!): Auth!
  logout(token: String!): Logout!
  refreshToken(token: String!): Token!
  signup(data: SignupInput!): Auth!
  updateEmail(data: UpdateEmailInput!): ChangeEmail!
  updateUser(data: UpdateUserInput!): User!
}

type Query {
  captcha: Captcha!
  emailCode(email: String!, name: String): Email!
  me: User!
  validateCaptcha(code: String!, key: String!): ValidateCaptcha!
}

"""User role"""
enum Role {
  ADMIN
  DIRECTOR
  SECRETARY
  TEACHER
  VICE_DIRECTOR
}

input SignupInput {
  college: String
  email: String!
  id: Int!
  name: String!
  password: String!
  roles: [Role!]
}

type Token {
  """JWT access token"""
  accessToken: String!

  """JWT refresh token"""
  refreshToken: String!
}

input UpdateEmailInput {
  key: String!
  userId: Int!
}

input UpdateUserInput {
  college: String
  name: String
}

type User {
  college: String

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  email: String!
  id: Int!
  name: String!
  roles: [Role!]

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

type ValidateCaptcha {
  code: String
  valid: Boolean!
}
